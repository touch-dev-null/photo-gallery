#create_new_gallery

  %h2= t('gallery.create_new_gallery')

  =form_for @new_gallery, :url => user_galleries_path(current_user) do |f|
    = f.error_messages

    =f.label :name
    =f.text_field :name

    =f.submit :value => "#{t('user.signin_button')}", :class => 'submit_button'